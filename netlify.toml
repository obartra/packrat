[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: the router uses history.pushState, so direct URL hits
# on non-root paths must serve index.html rather than 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
